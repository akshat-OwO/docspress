# Getting Started

Create `.mdx` files under `src/docs` and the plugin exposes them under `/docs`.

For this vanilla example, Docspress renders Markdown-compatible MDX to HTML. Framework component syntax is reserved for adapters like the React starter.
