# Guide

This nested page is `src/pages/guide/index.mdx`, so the generated route path is `/guide`.

Docspress follows TanStack Router-style file conventions for index routes, route tokens, dynamic params, and pathless segments.
