# Getting Started

The React starter uses `virtual:docspress/react-routes` to lazy-load MDX modules.

The framework-specific rendering comes from the React runtime and `@mdx-js/rollup`; the Vite plugin still owns docs discovery, slug generation, and route metadata.
