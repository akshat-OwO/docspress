# Getting Started

The React starter uses the framework-owned Docspress entrypoints to lazy-load MDX modules.

The Vite plugin owns page discovery, slug generation, SSR rendering, and route metadata.
