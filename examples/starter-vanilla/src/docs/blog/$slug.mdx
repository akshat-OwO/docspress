# Dynamic Blog Entry

This file is named `$slug.mdx`, so the generated manifest path is `/docs/blog/:slug`.

The first version exposes dynamic params in route metadata. A real app can use those params to load document data or choose a fallback page.
