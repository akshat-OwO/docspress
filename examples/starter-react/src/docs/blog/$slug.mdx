# Dynamic Blog Entry

The `$slug.mdx` filename becomes `/docs/blog/:slug` in the route manifest.

The React helper can match concrete browser paths like `/docs/blog/hello-world` against that pattern.
